# Danh sách công việc — 17/06/2026
**Người thực hiện:** Vinhdev  
**Mục tiêu:** Việt hóa toàn bộ thông báo console (log, warn, error) trong ứng dụng để đồng bộ và dễ theo dõi lỗi khi debug.

---

## VIỆT HÓA CONSOLE LOGS

### 1. Các file cấu hình & tiện ích (Utils, Constants, API, Socket)
- [ ] `src/utils/encryption.ts`
- [ ] `src/constants/Config.ts`
- [ ] `src/api/axiosClient.ts`
- [ ] `src/socket/SocketClient.ts`

### 2. Các file dịch vụ (Services)
- [ ] `src/services/authService.ts`
- [ ] `src/services/orderService.ts`
- [ ] `src/services/paymentService.ts`
- [ ] `src/services/ttsService.ts`

### 3. Các màn hình xác thực & tài khoản (Auth & Account Screens)
- [ ] `src/pages/auth/LoginScreen.tsx`
- [ ] `src/pages/auth/RegisterScreen.tsx`
- [ ] `src/pages/account/UpdateProfile/UpdateProfileScreen.tsx`
- [ ] `src/pages/account/ChangePassword/ChangePasswordScreen.tsx`
- [ ] `src/context/AuthContext.tsx`

### 4. Các màn hình chính & giỏ hàng (Home, Menu, Cart Screens)
- [ ] `src/pages/home/VendorListScreen.tsx`
- [ ] `src/pages/menu/MenuScreen.tsx`
- [ ] `src/pages/menu/ProductDetailScreen.tsx`
- [ ] `src/components/menu/ProductModal.tsx`
- [ ] `src/pages/cart/CartScreen.tsx`

### 5. Các màn hình đơn hàng & thanh toán (Order & Payment Screens)
- [ ] `src/pages/orders/OrderScreen.tsx`
- [ ] `src/pages/orders/OrderDetailScreen.tsx`
- [ ] `src/pages/orders/PaymentScreen.tsx`

---

## GHI CHÚ
- Việt hóa toàn bộ nội dung text truyền vào `console.log`, `console.warn`, `console.error`.
- Đảm bảo giữ nguyên các tham số động truyền kèm trong log (ví dụ: các biến, payload, error object).
- Giữ nguyên cấu trúc code logic, chỉ thay đổi chuỗi text log.

---

## SỬA LỖI HỆ THỐNG (BUILD APK & IN HÓA ĐƠN)
- [x] Sửa lỗi Metro bundler khi build APK: Ẩn/comment out các asset không tồn tại (`splash_bg.png`, `coffee_pattern.png`) trong `src/constants/Config.ts` làm nghẽn tiến trình đóng gói.
- [x] Sửa lỗi HTTP 400 (Validation failed) khi in hóa đơn: Tách biệt database ID số (`id`/`orderId`) và mã đơn hiển thị (`orderCode`) trên màn hình OrderScreen, ReceiptModal, component BillReceiptComponent và dịch vụ billService.
- [x] Thêm log lỗi API chi tiết tại `src/api/axiosClient.ts` và `src/services/billService.ts` để debug lỗi 400/4000.

---

## CẬP NHẬT TIẾN ĐỘ THỰC HIỆN CHI TIẾT (17/06/2026)
- [x] **Khắc phục lỗi crash ứng dụng sau thanh toán (Disconnect/TTS Crash)**:
  - Loại bỏ các callback tùy chọn (`onStart`, `onDone`, `onError`) khi gọi hàm phát âm thanh `Speech.speak()` trong `ttsService.ts`. Các callback này gây lỗi NullPointerException trên các luồng native của Android khi ứng dụng chuyển đổi ngữ cảnh, làm ứng dụng tự động đóng đột ngột (crash/thoát app).
- [x] **Khai báo quyền truy cập dịch vụ TTS (AndroidManifest.xml)**:
  - Thêm thẻ intent `<action android:name="android.intent.action.TTS_SERVICE"/>` để đảm bảo hệ điều hành Android 11+ cấp quyền Visibility cho dịch vụ đọc giọng nói tiếng Việt của Google.
- [x] **Bỏ fix cứng chi nhánh/bàn (Dynamic Branch & Tables)**:
  - Thay thế giá trị hardcode `branchId: 1` bằng việc lấy động `branchId` từ thông tin đăng nhập của người dùng (`useAuth().user.branchId`).
  - Cập nhật cơ chế này đồng bộ tại: `HomeScreen.tsx`, `MenuScreen.tsx`, `CartScreen.tsx` và `ScanQRScreen.tsx` để hỗ trợ quét mã gọi món và lấy thông tin bàn từ API động theo đúng chi nhánh đang làm việc.
- [x] **Tối ưu hóa UI chọn bàn ăn (Grid lưới 3 cột)**:
  - Thiết kế lại hoàn toàn Modal chọn bàn ăn trong giỏ hàng thành dạng Grid lưới 3 cột hiển thị trực quan trạng thái bàn Trống (Xanh) / Bận (Đỏ/Cam) và hiển thị checkmark cho bàn đang được lựa chọn.
- [x] **Khắc phục lỗi chuyển đổi ngôn ngữ Việt - Anh**:
  - Khắc phục lỗi các text hiển thị literal key bằng cách khai báo và đồng bộ toàn bộ các flat key được sử dụng trong mã nguồn vào root của `vi.json` và `en.json` (ví dụ: `all`, `add_note`, `checkout_now`, `search_placeholder`, v.v.).
  - Bổ sung hàm `setLanguage` trong `i18n.ts` giúp lưu trữ lựa chọn ngôn ngữ của người dùng vào `AsyncStorage` một cách nhất quán, tránh tình trạng tự động reset về tiếng Việt sau khi đóng/mở lại app.

